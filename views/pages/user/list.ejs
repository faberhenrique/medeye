<% include ../../templates/header.ejs %>
    <div class="container-fluid">
        <input class="form-control form-control-lg" type="text" placeholder="Digite o nome do usuário:">
        <ul class="list-group">
            <% users.forEach(function(customer){ %>
                <li class="list-group-item">

                    <span class="name">
    					<span class="info">
    						Name:
    					</span>
                        <%= customer.name %>
                    </span>
                    <a class="link-edit" href="/edit/<%= customer._id %>">
                        <i class="fa fa-edit"></i> Edit
                    </a>
                    <a class="link-delete" href="/delete/<%= customer._id %>" onclick="return confirm('Tem certeza que deseja excluir? / Are you sure you want to delete?');">
                    <i class="fa fa-close"></i> Delete
			</a>
                </li>
                <% }) %>
        </ul>
    </div>
<% include ../../templates/footer.ejs %>
